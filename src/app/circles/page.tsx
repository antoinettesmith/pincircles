import { CirclesClient } from "./CirclesClient";

export const metadata = {
  title: "Circles - PinCircles",
  description: "Browse interest communities",
};

export default function CirclesPage() {
  return <CirclesClient />;
}
