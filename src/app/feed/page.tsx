import { FeedClient } from "./FeedClient";

export const metadata = {
  title: "Feed - PinCircles",
  description: "Discover trending pins",
};

export default function FeedPage() {
  return <FeedClient />;
}
